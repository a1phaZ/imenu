extends ../layout

block content
	.page-header
		h1#page-header Заказ

	.row
		include order.list.mixin.pug
	hr
	.pull-right.total
		| Итого: #{inTotal}
		i.fa.fa-rub
	.pull-right.total
		| Итого с учетом скидки: #{inTotalWithDiscount}
		i.fa.fa-rub