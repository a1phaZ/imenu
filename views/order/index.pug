extends ../layout

block content
  .page-header
    h1#page-header Заказ
    include status

  include order.list.mixin.pug
  hr
  include order.history.list.mixin.pug  
  hr
  h4.text-right.total
    strong
      | Итого: 
      span#total 
        =inTotalWithDiscount+inTotalhistoryWithDiscount
    i.fa.fa-rub
  hr
  .row
    form.form-horizontal.col-sm-12#send-order(method='POST')
      input(type='hidden', name='_csrf', value=_csrf)
      .form-group
        label.col-xs-12.control-label(for='comment') Комментарий к заказу
        .col-xs-12
          textarea.form-control(name='comment', id='comment', placeholder='Комментарий к заказу', rows='7', required) #{orderComment}
          span.small
            | В комментарии к заказу 
            strong обязательно 
            | укажите номер столика и всё то, что сочтёте нужным указать дополнительно.
      
      .form-group(style=(orderStatus == 4)?'display:none;':'display:block;')
        .col-xs-6
          button.col-sm-12.btn.btn-success.form-control(type='submit', style=(historyList.length)?'display:none;':'display:block;')
            | Отправить заказ
        if orderStatus
          .col-xs-6
            a#order-plz(href='/order/'+orderId+'/close').btn.btn-default.form-control Попросить счёт
