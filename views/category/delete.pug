//- extends ../layout

//- block content
//-   header.text-center
//-     h1#page-header Удаление категории #{categoryTitle}

//-   form.form-horizontal(method='POST')
//-     input(type='hidden', name='_csrf', value=_csrf)
//-     .form-group
//-       .col-3.text-right
//-         mark
//-           | Вы можете удалить категорию, но это действие не обратимо. Будьте аккуратны.
//-       .col-6
//-         .form-group
//-           button.form-control.btn.btn-primary(type='submit', disabled=!isAdmin)
//-             i.fa.fa-floppy-o
//-             | Удалить категорию
//-         .form-group
//-           a.form-control.btn.btn-link(href='/category')
//-             i.fa.fa-floppy-o
//-             | Отменить удаление

mixin delete-category()
  .modal#delete-modal
    a(href="#modals", class="modal-overlay", aria-label="Close")
    .modal-container(role="document")
      .modal-header
        a(href="#modals", class="btn btn-clear float-right", aria-label="Close")
        .modal-title Удаление категории #{categoryTitle}
      .modal-body
        .content
          p Вы можете удалить категорию, но это действие не обратимо. Будьте аккуратны.
      .modal-footer
        a(href="#modals" class="btn btn-link") Отмена
        if isAdmin
          button.btn.btn-primary(type='submit', disabled=!isAdmin) Удалить

+delete-category()