mixin category-menu(category)
  li.col-xs-6.col-sm-4
    ul
      li.dropdown-header.text-center #{category.title}
      li
        .random.text-center
          a(href='/category/'+category.slug)
            img.category-logo(src='/uploads/'+category.logo)

for category in categoryList
  +category-menu(category)