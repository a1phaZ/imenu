extends ../layout

block content
  header.text-center
    h2#page-header Забыли пароль?

  form(method='POST')
    input(type='hidden', name='_csrf', value=_csrf)
    .form-group
      p Введите Ваш адрес электронной почты и мы вышлем Вам инструкции для сброса пароля.
      label.form-label(for='email') Email
      input.form-input(type='email', name='email', id='email', placeholder='Email', autofocus, required)
    .form-group
      button.btn.btn-primary(type='submit')
        i.fa.fa-key
        | Сбросить пароль
